---
import { Github, Twitter, Instagram, ArrowUpRight } from 'lucide-react';
---

<footer class="bg-black border-t-8 border-neo-teal py-20 relative overflow-hidden">
  
  <div class="container mx-auto px-6 relative z-10">
    <div class="flex flex-col md:flex-row justify-between items-start md:items-center gap-12 mb-16">
      <!-- Logo -->
      <div>
        <h2 class="text-9xl font-black italic tracking-tighter text-white/5 absolute -top-12 -left-12 select-none pointer-events-none">DKT</h2>
        <h2 class="text-6xl font-black italic tracking-tighter text-white mb-2 relative z-10">DKT<span class="text-neo-teal">.</span></h2>
        <p class="text-white font-mono font-bold max-w-xs border-l-4 border-neo-red pl-4">Fikirlerin özgürce dolaştığı, zihinlerin çarpıştığı dijital meydan.</p>
      </div>

      <!-- Links -->
      <div class="flex gap-4">
        {[
            { icon: Twitter, href: "#" },
            { icon: Github, href: "#" },
            { icon: Instagram, href: "#" }
        ].map((social) => (
            <a href={social.href} class="w-16 h-16 bg-white border-4 border-transparent flex items-center justify-center text-black hover:bg-neo-teal hover:border-white hover:shadow-none shadow-[4px_4px_0px_0px_white] transition-all duration-300">
                <social.icon className="w-8 h-8" />
            </a>
        ))}
      </div>
    </div>

    <div class="grid grid-cols-2 md:grid-cols-4 gap-8 mb-16 border-t-4 border-white pt-12">
        {['Manifesto', 'Kütüphane', 'Atölyeler', 'İletişim'].map((item) => (
            <a href="#" class="text-white font-black text-xl hover:text-neo-yellow transition-colors flex items-center gap-2 group uppercase">
                {item}
                <ArrowUpRight className="w-5 h-5 opacity-0 group-hover:opacity-100 transition-opacity text-neo-teal" />
            </a>
        ))}
    </div>

    <!-- Copyright -->
    <div class="flex flex-col md:flex-row justify-between items-center pt-8 border-t-2 border-white/20 text-xs font-bold text-white uppercase tracking-widest">
      <p class="bg-neo-red px-2 py-1 border border-white">&copy; {new Date().getFullYear()} DÜŞÜNCE VE KİTAP TOPLULUĞU.</p>
      <p class="bg-neo-teal text-black px-2 py-1 border border-white mt-4 md:mt-0">SCIENTIA POTENTIA EST</p>
    </div>
  </div>
</footer>
