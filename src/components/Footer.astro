---
import { Github, Twitter, Instagram, ArrowUpRight } from 'lucide-react';
---

<footer class="bg-slate-900 border-t border-white/10 py-20 relative overflow-hidden">
  
  <div class="container mx-auto px-6 relative z-10">
    <div class="flex flex-col md:flex-row justify-between items-start md:items-center gap-12 mb-16">
      <!-- Logo -->
      <div>
        <h2 class="text-8xl font-black italic tracking-tighter text-white/5 absolute -top-12 -left-12 select-none pointer-events-none">DKT</h2>
        <h2 class="text-4xl font-black italic tracking-tighter text-white mb-2 relative z-10">DKT<span class="text-teal-500">.</span></h2>
        <p class="text-slate-400 font-medium max-w-xs">Fikirlerin özgürce dolaştığı, zihinlerin çarpıştığı dijital meydan.</p>
      </div>

      <!-- Links -->
      <div class="flex gap-4">
        {[
            { icon: Twitter, href: "#" },
            { icon: Github, href: "#" },
            { icon: Instagram, href: "#" }
        ].map((social) => (
            <a href={social.href} class="w-12 h-12 bg-white/5 border border-white/10 rounded-full flex items-center justify-center text-slate-400 hover:bg-white hover:text-slate-900 hover:scale-110 transition-all duration-300">
                <social.icon class="w-5 h-5" />
            </a>
        ))}
      </div>
    </div>

    <div class="grid grid-cols-2 md:grid-cols-4 gap-8 mb-16 border-t border-white/10 pt-12">
        {['Manifesto', 'Kütüphane', 'Atölyeler', 'İletişim'].map((item) => (
            <a href="#" class="text-slate-400 font-bold hover:text-white transition-colors flex items-center gap-2 group">
                {item}
                <ArrowUpRight class="w-4 h-4 opacity-0 group-hover:opacity-100 transition-opacity text-teal-500" />
            </a>
        ))}
    </div>

    <!-- Copyright -->
    <div class="flex flex-col md:flex-row justify-between items-center pt-8 border-t border-white/5 text-xs font-bold text-slate-600 uppercase tracking-widest">
      <p>&copy; {new Date().getFullYear()} DÜŞÜNCE VE KİTAP TOPLULUĞU.</p>
      <p>SCIENTIA POTENTIA EST</p>
    </div>
  </div>
</footer>
