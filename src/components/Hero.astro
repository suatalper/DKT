---
import { ArrowRight, BookOpen, Brain, PenTool } from 'lucide-react';
---

<section class="relative min-h-screen flex items-center justify-center overflow-hidden pt-32 pb-20 bg-neutral-50">
  <!-- Grid Background -->
  <div class="absolute inset-0 z-0 opacity-20 bg-grid-pattern"></div>

  <!-- 2D Stickers (Emojis/Shapes) -->
  <div class="absolute inset-0 z-0 pointer-events-none select-none overflow-hidden">
    <div class="absolute top-1/4 right-[5%] text-9xl animate-shake" style="animation-duration: 3s;">
      🧠
    </div>
    <div class="absolute bottom-1/4 left-[5%] text-9xl animate-shake" style="animation-duration: 4s; animation-delay: 1s;">
      📚
    </div>
    <div class="absolute top-1/3 left-[15%] w-16 h-16 bg-primary border-4 border-black rounded-full animate-bounce"></div>
    <div class="absolute bottom-1/3 right-[20%] w-24 h-24 bg-secondary border-4 border-black rotate-12"></div>
  </div>

  <div class="container mx-auto px-6 relative z-10 text-center">
    
    <!-- Headline -->
    <div class="mb-12 relative inline-block">
      <h1 class="text-5xl md:text-8xl font-black tracking-tighter leading-none text-black drop-shadow-sm transform -rotate-2">
        FİKİRLERİN<br/>
        <span class="bg-primary text-black px-4 border-4 border-black shadow-brutal inline-block transform rotate-2 mt-4">AĞIRLIĞI</span><br/>
        YOKTUR
      </h1>
      
      <p class="text-xl md:text-2xl font-bold bg-white border-4 border-black shadow-brutal p-6 mt-12 max-w-3xl mx-auto transform rotate-1">
        Okuyan, düşünen ve üretenlerin <span class="text-secondary underline decoration-4 decoration-black">buluşma noktası.</span>
      </p>
    </div>

    <!-- CTA Buttons -->
    <div class="flex flex-col md:flex-row items-center justify-center gap-8 mt-8">
      <a href="#join" class="group px-8 py-4 bg-accent border-4 border-black text-black font-black text-2xl shadow-brutal hover:shadow-none hover:translate-x-1 hover:translate-y-1 transition-all flex items-center gap-3">
        KATIL! <ArrowRight className="w-8 h-8 border-2 border-black rounded-full p-1 bg-white" />
      </a>

      <a href="#workshops-section" class="px-8 py-4 bg-white border-4 border-black text-black font-bold text-xl shadow-brutal hover:shadow-none hover:translate-x-1 hover:translate-y-1 transition-all flex items-center gap-2">
        ATÖLYELER
      </a>
    </div>
    
    <!-- Meta -->
    <div class="mt-16 inline-block bg-black text-white px-4 py-2 font-mono font-bold text-sm transform -rotate-1">
        EST. 2024 // KÜTÜPHANE // ATÖLYE
    </div>
  </div>
</section>
