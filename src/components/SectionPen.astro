---
import { PenTool, Send, Sparkles } from 'lucide-react';
---

<section id="pen-section" class="py-24 bg-slate-900 relative border-t border-white/5">
    <div class="container mx-auto px-6 max-w-6xl">
        
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
            
            <!-- Text Content -->
            <div>
                <div class="flex items-center gap-2 mb-4">
                    <div class="w-8 h-1 bg-linear-to-r from-red-500 to-transparent"></div>
                    <span class="text-red-500 font-bold uppercase tracking-widest text-sm">Scriptorium</span>
                </div>
                <h2 class="text-5xl md:text-6xl font-black text-white mb-6 leading-tight">
                    KELİMELERİN <br/> <span class="text-transparent bg-clip-text bg-linear-to-r from-red-500 to-orange-500">GÜCÜ</span>
                </h2>
                <p class="text-slate-400 text-lg mb-8 leading-relaxed">
                    Düşüncelerini serbest bırak. Haftalık meydan okumalara katıl, topluluğun bir parçası ol ve sınırlarını zorla.
                </p>
                <div class="flex gap-4">
                    <button class="px-8 py-4 bg-red-600 hover:bg-red-700 text-white font-bold rounded-xl transition-all shadow-lg shadow-red-600/20 flex items-center gap-2">
                        <PenTool class="w-5 h-5" /> Yazmaya Başla
                    </button>
                    <button class="px-8 py-4 bg-white/5 border border-white/10 hover:bg-white/10 text-white font-bold rounded-xl transition-all">
                        Son Yazılar
                    </button>
                </div>
            </div>

            <!-- Glass Challenge Card -->
            <div class="relative">
                <!-- Glow Effects -->
                <div class="absolute inset-0 bg-linear-to-br from-red-600 to-purple-600 rounded-[2.5rem] blur-2xl opacity-20 animate-pulse"></div>
                
                <div class="relative bg-white/5 backdrop-blur-2xl border border-white/10 rounded-[2.5rem] p-10 transform rotate-1 hover:rotate-0 transition-transform duration-500">
                    <div class="flex justify-between items-start mb-8">
                        <div>
                            <span class="px-3 py-1 bg-red-500/20 text-red-400 text-xs font-black uppercase rounded-lg tracking-widest">Haftalık Challenge</span>
                            <h3 class="text-3xl font-black text-white mt-3 italic">"Sessiz Çığlık"</h3>
                        </div>
                        <Sparkles class="w-8 h-8 text-yellow-400" />
                    </div>

                    <p class="text-slate-300 font-medium mb-8 leading-relaxed border-l-4 border-red-500 pl-6">
                        Hiçbir diyaloğun olmadığı, sadece tasvirlerin konuştuğu bir sahne yaz. Gürültüyü kelimelerinle sustur.
                    </p>

                    <div class="flex items-center justify-between p-6 bg-slate-900/50 rounded-2xl border border-white/5">
                        <div class="flex -space-x-3">
                            {[1,2,3].map(i => (
                                <div class={`w-10 h-10 rounded-full border-2 border-slate-900 bg-linear-to-br from-slate-700 to-slate-600`}></div>
                            ))}
                            <div class="w-10 h-10 rounded-full border-2 border-slate-900 bg-slate-800 flex items-center justify-center text-xs font-bold text-white">+42</div>
                        </div>
                        <div class="text-right">
                             <p class="text-xs text-slate-500 uppercase font-bold">Bitiş</p>
                             <p class="text-white font-mono font-bold">02:14:59</p>
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </div>
</section>
