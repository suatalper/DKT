---
import { PenTool, Feather, Send } from 'lucide-react';
---

<section id="pen-section" class="py-24 bg-swiss-bg border-b border-gray-200">
  <div class="container mx-auto px-6">
    <div class="flex items-start gap-4 mb-20 border-l-8 border-secondary pl-6">
      <h2 class="text-4xl md:text-5xl font-black text-swiss-text tracking-tighter uppercase">Yazın &<br/>Üretim.</h2>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-0 border border-gray-200">
        <!-- Creative Writing Challenge (Prominent Card) -->
        <div class="lg:col-span-1 p-12 bg-secondary text-white flex flex-col justify-between">
             <div>
                 <div class="flex items-center gap-3 mb-8">
                     <Feather className="w-8 h-8 text-white" />
                     <span class="font-bold tracking-widest uppercase text-sm border border-white px-2 py-1">Haftanın Meydan Okuması</span>
                 </div>
                 
                 <h3 class="text-4xl font-black mb-6 tracking-tight leading-none">"SESSİZLİĞİN SESİ"</h3>
                 <p class="text-white/90 text-lg font-medium leading-relaxed mb-12">
                     Bu hafta, hiç konuşmadan anlaşan iki karakterin hikayesini yazıyoruz. 
                     Maksimum 500 kelime.
                 </p>
             </div>
             
             <div class="space-y-6">
                 <div class="flex justify-between text-sm font-bold uppercase tracking-wider border-t border-white/20 pt-4">
                     <span>Kalan Süre:</span>
                     <span class="font-mono">2 Gün 4 Saat</span>
                 </div>
                 <button class="w-full py-4 bg-white text-secondary hover:bg-gray-100 uppercase font-black tracking-tight transition-colors flex items-center justify-center gap-2">
                     <Send className="w-5 h-5" />
                     Hikayeni Gönder
                 </button>
             </div>
        </div>

        <!-- User Blogs -->
        <div class="lg:col-span-2 grid grid-cols-1 md:grid-cols-2 bg-white">
            {[1, 2, 3, 4].map((i) => (
                <div class="p-8 border-b md:border-b-0 border-gray-200 even:border-l hover:bg-swiss-bg transition-colors flex flex-col h-full group">
                    <div class="flex items-center gap-4 mb-6">
                        <div class="w-10 h-10 rounded-none bg-swiss-text"></div>
                        <div>
                            <h5 class="text-sm font-bold text-swiss-text uppercase tracking-wider">Kullanıcı {i}</h5>
                            <span class="text-xs font-medium text-swiss-muted">2 saat önce</span>
                        </div>
                    </div>
                    <h4 class="text-xl font-bold text-swiss-text mb-4 group-hover:text-secondary transition-colors leading-tight">Modern Çağda Stoacılık</h4>
                    <p class="text-swiss-text/70 leading-relaxed mb-6 grow font-medium">
                        Günümüzün kaosunda iç huzuru bulmak için antik öğretilere nasıl dönebiliriz? 
                        Marcus Aurelius'un günlüklerinden notlar...
                    </p>
                    <a href="#" class="inline-block mt-auto text-sm font-bold text-secondary uppercase tracking-wider hover:underline">Devamını Oku →</a>
                </div>
            ))}
        </div>
    </div>
  </div>
</section>
