---
import { Brain, Swords, Milestone, Zap } from 'lucide-react';
---

<section id="brain-section" class="py-24 relative overflow-hidden bg-slate-900">
  
  <div class="container mx-auto px-6 relative z-10">
    <div class="mb-12 text-center md:text-left">
      <h2 class="text-5xl md:text-7xl font-black text-white mb-6 uppercase tracking-tighter">
        FİKİR <span class="text-transparent bg-clip-text bg-linear-to-r from-teal-400 to-red-500">ARENASI</span>
      </h2>
      <p class="text-slate-400 text-xl font-medium max-w-2xl">Zıtlıkların harmonisi. Tez ve antitez, yeni bir gerçeklikte buluşuyor.</p>
    </div>

    <!-- Dashboard / Bento Layout -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
       
       <!-- Main Debate Widget -->
       <div class="md:col-span-2 bg-white/5 backdrop-blur-3xl border border-white/10 rounded-3xl p-8 relative overflow-hidden group">
          <div class="absolute inset-0 bg-linear-to-r from-teal-500/10 to-red-500/10 opacity-0 group-hover:opacity-100 transition-opacity duration-700"></div>
          
          <div class="flex items-center gap-3 mb-8">
             <div class="px-3 py-1 bg-red-500/20 border border-red-500/30 rounded-full text-red-500 text-xs font-bold uppercase tracking-widest animate-pulse">Canlı Tartışma</div>
          </div>

          <div class="flex flex-col md:flex-row gap-8 items-center justify-between relative z-10">
             <div class="text-center md:text-left flex-1">
                <h3 class="text-2xl font-bold text-white mb-2">Yapay Zeka Sanatı</h3>
                <div class="h-1 w-24 bg-linear-to-r from-teal-500 to-transparent rounded-full mb-4 mx-auto md:mx-0"></div>
                <p class="text-slate-400 text-sm font-medium">"Yaratıcılığın sonu mu, yoksa yeni bir başlangıç mı?"</p>
             </div>
             
             <div class="w-16 h-16 flex items-center justify-center bg-slate-900 rounded-full border border-white/20 shadow-xl z-10">
                <Swords class="text-white w-6 h-6" />
             </div>

             <div class="text-center md:text-right flex-1">
                <h3 class="text-2xl font-bold text-white mb-2">İnsan Dokunuşu</h3>
                 <div class="h-1 w-24 bg-linear-to-l from-red-500 to-transparent rounded-full mb-4 mx-auto md:ml-auto"></div>
                <p class="text-slate-400 text-sm font-medium">"Duygu aktarımı olmadan sanat eksik kalır."</p>
             </div>
          </div>

          <!-- Progress Bar Visual -->
          <div class="mt-12 flex items-center gap-4">
             <span class="text-teal-400 font-bold text-sm">65%</span>
             <div class="flex-1 h-3 bg-slate-800 rounded-full overflow-hidden relative">
                 <div class="absolute inset-y-0 left-0 w-[65%] bg-linear-to-r from-teal-500 to-teal-300"></div>
             </div>
             <span class="text-red-500 font-bold text-sm">35%</span>
          </div>
       </div>

       <!-- Vertical Topics Stack -->
       <div class="md:col-span-1 flex flex-col gap-6">
          {['Tekno-Felsefe', 'Dijital Etik', 'Gelecek Simülasyonları'].map((topic, i) => (
             <div class="flex-1 bg-white/5 backdrop-blur-xl border border-white/10 rounded-3xl p-6 flex items-center justify-between hover:bg-white/10 hover:border-teal-500/30 transition-all cursor-pointer group">
                <div>
                    <h4 class="text-lg font-bold text-white group-hover:text-teal-400 transition-colors">{topic}</h4>
                    <span class="text-xs text-slate-500 uppercase tracking-wider font-bold">Keşfet</span>
                </div>
                <Zap class={`w-5 h-5 ${i === 0 ? 'text-teal-400' : i === 1 ? 'text-red-500' : 'text-purple-500'} opacity-80`} />
             </div>
          ))}
       </div>

    </div>
  </div>
</section>
